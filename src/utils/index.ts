import { calculateAge, dateFormat } from "./date";
import { isErrorProps } from "./isError";
import { loadFromLocalStorage, removeFromLocalStorage, saveToLocalStorage } from "./localStorage";

export {
    calculateAge,
    dateFormat,
    isErrorProps,
    loadFromLocalStorage,
    removeFromLocalStorage,
    saveToLocalStorage
}